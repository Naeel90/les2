def  new_password(oldpassword, newpassword):
    if newpassword != oldpassword and len(newpassword [5:]):

      return True
    else:
        return False

print(new_password('asdasd123',input('nieuwe password:')))
